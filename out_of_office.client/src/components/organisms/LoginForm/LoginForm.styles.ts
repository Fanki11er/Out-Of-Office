import styled from "styled-components";
import { StyledDefaultForm } from "../../atoms/StyledDefaultForm/StyledDefaultForm.styles";

export const StyledLoginForm = styled(StyledDefaultForm)`
  flex-direction: column;
  gap: 1rem;
  width: fit-content;
  height: fit-content;
`;
